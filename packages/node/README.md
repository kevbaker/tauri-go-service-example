# Node Packages

Reusable TypeScript packages belong here. The first package is [`task-core-library`](task-core-library/README.md), which owns the transport-neutral task contracts, CRUD client, protocol parsing, and normalized errors.

Future packages may include generated API contract bindings, configuration types, and genuinely shared test utilities.

Application-specific React components remain in `apps/task-app`.
