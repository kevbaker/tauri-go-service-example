# Go Packages

Reusable Go packages that are shared across services belong here. Code used only by the task service remains under `services/go/task-service/internal`.

Do not extract shared packages until at least one concrete reuse case exists.
