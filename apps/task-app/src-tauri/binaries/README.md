# Generated sidecar binaries

`npm run sidecar:build --workspace task-app` builds the Go task service here using Tauri's required target-triple filename. Generated executables are ignored by Git and must not be committed.
